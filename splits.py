# default splits at space
variable.split()

# split at the colon
variable.split(':')

# split at comma
variable.split(',')

# split at length of number x
splits = variable[i:i+3] for i in range(0, len(variable), 3)])
